# Git prikazy

```bash
# pridat zmeny, commitnout zmeny, pushnout commit na server
git add .
git commit -m "save, nebo zprava"
git push

# nebo rovnou:

git add . && git commit -m "save" && git push


# smazani zmen a obnoveni posledniho commitu:
git reset HEAD --hard
```
